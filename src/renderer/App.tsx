/**
 * SnapBack — React renderer entry point (placeholder).
 *
 * Full UI implementation is covered in Tasks 13–15.
 */

import React from 'react';

export default function App(): React.ReactElement {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>SnapBack Productivity Suite</h1>
      <p>Loading…</p>
    </div>
  );
}
